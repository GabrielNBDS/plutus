@set('color', active ? 'text-orange-400' : 'text-neutral-800')

<li>
  <a href="{{ href }}" class="flex items-center {{ color }} gap-1 font-medium">
    <i class="text-2xl">
      @svg(icon)
    </i>

    <p>
      {{ text }}
    </p>
  </a>
</li>
